import io.reactivex.Observable

/*
fun main() {
    Observable.just("this is","a test", "lol")
        //.map { ss -> ss.split(" ") }
        .doOnNext{i -> println(" this is doOnNext: $i")}
        .flatMap { ss -> Observable.fromIterable(ss.split(" "))}
        .subscribe { i -> println(i)}
}*/
